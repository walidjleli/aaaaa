{{ form_start(form) }}
<div class="mb-3">
    {{ form_widget(form) }}
</div>
<button type="submit" class="btn btn-primary btn-lg">
    <i class="zmdi zmdi-save"></i> {{ button_label|default('Enregistrer') }}
</button>
{{ form_end(form) }}